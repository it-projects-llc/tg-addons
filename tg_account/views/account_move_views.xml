<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <record id="action_duplicate_to_fiscal_company" model="ir.actions.server">
        <field name="name">Duplicate to fiscal invoice</field>
        <field name="model_id" ref="account.model_account_move" />
        <field name="groups_id" eval="[(4, ref('account.group_account_user'))]" />
        <field name="binding_model_id" ref="account.model_account_move" />
        <field name="state">code</field>
        <field
            name="code"
        >action = records._action_duplicate_to_fiscal_company()</field>
    </record>

    <record id="move_view_form" model="ir.ui.view">
        <field name="name">account.move.form.tg</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form" />
        <field name="arch" type="xml">
            <xpath
                expr="//group[@name='accounting_info_group']/field[@name='company_id']"
                position="after"
            >
                <field name="must_be_duplicated" />
                <field name="duplicated_fiscal_invoice" />
            </xpath>
        </field>
    </record>
</odoo>

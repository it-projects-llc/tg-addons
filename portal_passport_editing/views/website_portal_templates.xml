<?xml version="1.0" encoding="utf-8"?>
<!--Copyright 2019 Kolushov Alexandr <https://it-projects.info/team/KolushovAlexandr>
    License LGPL-3.0 or later (http://www.gnu.org/licenses/lgpl.html). -->
<odoo>

    <template id="details_passport" inherit_id="portal.portal_my_details">
        <xpath expr="//select[@name='state_id']/.." position="after">
            <br />
            <div t-attf-class="form-group col-lg-12">
                <h1>Identification</h1>
            </div>
            <br />
            <div t-attf-class="form-group form-check #{error.get('passport') and 'o_has_error' or ''} col-lg-6">
                <label class="control-label" for="passport">Passport</label>
                <input type="text" name="passport" t-attf-class="form-control #{error.get('passport') and 'is-invalid' or ''}" t-att-value="passport or partner.passport" />
                <div class="form-check" t-if="show_passport_checkbox">
                    <input class="form-check-input" type="checkbox" id="is_passport_not_ready" name="is_passport_not_ready" t-att-checked="is_passport_not_ready or partner.is_passport_not_ready"/>
                    <label class="form-check-label" for="is_passport_not_ready">Passport is not ready</label>
                </div>
            </div>
            <div t-attf-class="form-group #{error.get('birthdate_date') and 'o_has_error' or ''} col-lg-6">
                <label class="control-label" for="birthdate_date">Date of Birth</label>
                <input type="date" name="birthdate_date" t-attf-class="form-control #{error.get('birthdate_date') and 'is-invalid' or ''}" t-att-value="birthdate_date or partner.birthdate_date" />
            </div>
            <div t-attf-class="form-group #{error.get('nationality_id') and 'o_has_error' or ''} col-lg-6">
                <label class="control-label" for="nationality_id">Country of Citizenship</label>
                <select name="nationality_id" t-attf-class="form-control #{error.get('nationality_id') and 'is-invalid' or ''}">
                    <option value="">Country of Citizenship</option>
                    <t t-foreach="countries or []" t-as="country">
                        <option t-att-value="country.id" t-att-selected="country.id == int(country_id) if country_id else country.id == partner.country_id.id">
                            <t t-esc="country.name" />
                        </option>
                    </t>
                </select>
            </div>
            <br />
        </xpath>
    </template>

</odoo>
